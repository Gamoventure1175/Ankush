{
  "name": "my-fullstack-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd frontend && npm install && npm run build && cd ../backend && npm install && node index.js ", 
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run dev\""
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
